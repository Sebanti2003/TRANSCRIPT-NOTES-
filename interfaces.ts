interface user{
    email:string,
    useid:number
}
interface user{
    username:string
}
const use:user={
    email:"h@h.com",
    useid:39,
    username:"sebanti"
}
console.log(use);

export {}
//merging properties are inherited in the case of interfaces
