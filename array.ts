const array:number[]=[2,3,4];
const numbr:number|undefined=array.pop()
const numbber:number[][]=[//it means than it is a array of array of numbers
    [3.43,2],
    [2,3]
]
const arr:(string|number)[]=[4,3,"home"]//it means not only the array can store only number or only string ...it can store both at the same time
console.log(arr);

export {}
